{
  "name": "github-app-token-exchange-action",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Composite GitHub Action that exchanges a workflow OIDC token for a scoped GitHub App installation token.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
